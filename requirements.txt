recipe-scrapers>=15.11.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
truststore>=0.9      # verify TLS against the OS trust store, not just certifi,
                     # so corporate TLS interception doesn't break every fetch
openpyxl>=3.1        # for the Velt seizoenskalender xlsx, once it arrives
